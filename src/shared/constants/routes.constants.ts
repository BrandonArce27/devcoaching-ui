export enum GlobalRoute {
  AUTH = "auth",
  APP = "app",
  MODAL = "modal",
  NOT_FOUND = "notFound",
}

export enum AppScreen {
  ROOT = "root",
  HOME = "home",
  PROFILE = "profile",
  REWARD = "reward",
}

export enum RootScreen {
  HOME = "home",
  PROFILE = "profile",
}

export enum AuthScreen {
  LAUNCH = "launch",
  LOGIN = "login",
}
